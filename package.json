{
  "name": "ty-project-curabot",
  "version": "1.0.0",
  "private": true,
  "description": "Deployment helper scripts for the CURABOT monorepo",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm install --prefix Backend && npm install --prefix Frontend && npm run build --prefix Frontend",
    "build": "npm run build --prefix Frontend",
    "start": "npm run start --prefix Backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "engines": {
    "node": ">=22.0.0 <23.0.0"
  }
}
